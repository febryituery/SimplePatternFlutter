apply plugin: 'com.android.application'

android {
    signingConfigs {
        develop {
            keyAlias 'key'
            keyPassword '123456'
            storeFile file('key.jks')
            storePassword 'kesan123'
        }
        tes {
            keyAlias 'key'
            keyPassword '123456'
            storeFile file('key.jks')
            storePassword 'kesan123'
        }
        release {
            keyAlias 'key'
            keyPassword '123456'
            storeFile file('key.jks')
            storePassword 'kesan123'
        }
    }
}
